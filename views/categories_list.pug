extends layout

block content
    div.header
        h1= title
        a.btn(href='/catalog/category/create') Create new category

    ul
        each category in categories_list
            li
                a(href=category.url) #{category.name}
        else
            li There are no categories.