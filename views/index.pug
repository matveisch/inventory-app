extends layout

block content
  h1= title
  p Welcome to #{title}

  if err
    p Error getting dynamic content.
  else
    p The inventory has the following record counts:

    ul
      li #[strong Items:] !{results.item_count}
      li #[strong Categories:] !{results.category_count}